# FlowFM-Inspector Tools

The FlowFM-Inspector tools package provides the necessary tooling to automate building and packaging the FlowFM-Inspector application.

## Commands

* generate-directories-fragment: Generate the directory structure as directories.wxs.
* generate-component-fragment: Generate a component fragment.wxs file listing all the components which should be included within this fragment.

## Templates
